# Leave empty to use the Vite dev proxy (recommended in development).
# In production, set this to the deployed FastAPI base URL, e.g.
# VITE_API_BASE_URL=https://api.stocksense.example.com
VITE_API_BASE_URL=
