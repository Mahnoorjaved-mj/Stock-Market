# StockSense — Python dependencies
#
# Fresh install:
#   pip install -r requirements.txt
#
# This installs CPU-only torch (~200 MB) for the LSTM predictor. If you
# need GPU support, replace the torch line with the appropriate cu121
# wheel from https://pytorch.org/get-started/locally/
#
# Notes:
# - The app gracefully falls back to synthetic predictions when torch
#   is missing, so on tiny hosts you can omit it.
# - psycopg2-binary is the only DB driver — Postgres is required.

# ---- Web framework ----
Flask>=2.3,<4
Flask-Cors>=4
Flask-WTF>=1.2
Flask-Limiter>=3.5
Jinja2>=3.1
itsdangerous>=2.1
Werkzeug>=2.3

# ---- Auth / email ----
bcrypt>=4.0
email-validator>=2.0
pyotp>=2.9

# ---- Database ----
psycopg2-binary>=2.9
alembic>=1.13

# ---- Caching / rate-limit storage ----
redis>=5.0

# ---- Market data + numeric stack ----
requests>=2.31
yfinance>=0.2
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pytz

# ---- AI predictions (LSTM via PyTorch CPU build) ----
# Comment this out to skip AI; the app will fall back to synthetic data.
torch>=2.0

# ---- Scheduling ----
APScheduler>=3.10

# ---- Notifications (web push) ----
pywebpush>=2.0

# ---- Production server ----
waitress>=3.0
gunicorn>=21.2; sys_platform != "win32"

# ---- Observability ----
sentry-sdk[flask]>=1.40
python-json-logger>=2.0

# ---- Config ----
python-dotenv>=1.0

# ---- Dev / test (optional, used by `pytest` and CI) ----
pytest>=7.4
pytest-cov>=4.1
ruff>=0.3
black>=24.0
